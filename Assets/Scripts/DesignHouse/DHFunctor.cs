using UnityEngine;
using System.Collections;

// TBD: Some functor will take effect to enhance the mesh object, or just use elem instead of functor
// The others will be added to a mesh object as a component, which should use connector
public class DHFunctor : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
